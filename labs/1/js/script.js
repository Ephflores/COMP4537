// This code was developed with assistance from OpenAI's ChatGPT.

window.addEventListener("DOMContentLoaded", () => {
  document.getElementById("pageTitle").textContent = MESSAGES.title;
  document.getElementById("studentName").textContent = MESSAGES.studentName;
  document.getElementById("writerBtn").textContent = MESSAGES.buttons.writer;
  document.getElementById("readerBtn").textContent = MESSAGES.buttons.reader;
});
